{
	"default": {
		"ENGINE": "django.db.backends.postgresql_psycopg2",
		"NAME": "{{ parts["project"].goods_database_name }}",
		"USER": "{{ parts["project"].goods_database_user }}",
		"PASSWORD": "{{ parts["project"].goods_database_password }}",
		"HOST": "{{ parts["project"].goods_database_host }}",
		"PORT": {{ parts["project"].goods_database_port }}
	}
}
